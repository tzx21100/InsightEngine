#include "Pch.h"
#include "CoreEngine.h"

namespace IS 
{
	/*Manifold::Manifold() {
		mBodyA = &RigidBody();
		mBodyB = RigidBody();
		mNormal = Vector2D();
		mDepth = 0.f;
		mContact1 = Vector2D();
		mContact2 = Vector2D();
		mContactCount = 0;
	}*/

	/*Manifold::Manifold(RigidBody const& bodyA, RigidBody const& bodyB,
		Vector2D normal, float depth,
		Vector2D contact1, Vector2D contact2, int contactCount) {

		mBodyA = bodyA;
		mBodyB = bodyB;
		mNormal = normal;
		mDepth = depth;
		mContact1 = contact1;
		mContact2 = contact2;
		mContactCount = contactCount;
	}*/
}