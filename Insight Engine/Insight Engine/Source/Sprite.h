#pragma once
#include "Graphics.h"
namespace IS {
    class Sprite :public IComponent{

    public:
        //model type
        ModelType model = ModelType::Box;

        //funny things u want the sprite to have like texture
        //texture

    };
}